@page
@model RoomBooking.Pages.BookingControls.CancelBookingModel
<div class="cancelBooking-form">
    <h2>Отмена брони</h2>

    @if (!string.IsNullOrEmpty(Model.ErrorMessage))
    {
        <div class="alert alert-danger">@Model.ErrorMessage</div>
    }

    <form method="post">
        <div class="form-group">
            <label>ID брони</label>
            <input asp-for="BookingId" type="number" step="1" min="1" max="2147483647" class="form-control" />
        </div>
        <button type="submit" class="btn btn-primary">Отменить бронь</button>
    </form>
</div>